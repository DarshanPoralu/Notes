class Notes{
  String noteID;
  String title;
  DateTime createDateTime;
  DateTime latestEditDateTime;

  Notes({required this.noteID, required this.title, required this.createDateTime, required this.latestEditDateTime});
}